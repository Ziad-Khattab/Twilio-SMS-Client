<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- SPA fallback: forwards non-API routes to index.html (must run first) -->
    <filter>
        <filter-name>spaFilter</filter-name>
        <filter-class>com.twilio.twilio_project.SpaFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>spaFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Auth guard for protected endpoints -->
    <filter>
        <filter-name>authFilter</filter-name>
        <filter-class>com.twilio.twilio_project.AuthFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>authFilter</filter-name>
        <url-pattern>/dashboard</url-pattern>
        <url-pattern>/profile</url-pattern>
        <url-pattern>/admin/*</url-pattern>
    </filter-mapping>
</web-app>